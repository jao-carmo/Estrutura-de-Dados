//void atualizarAltura(struct Pessoa *p) {
//    p->altura *= 1.1; // Aumenta a altura em 10%
//}
//Por que a função deve receber um ponteiro em vez de uma struct comum?
//Para evitar a cópia de toda a struct, economizando memória e tempo de execução.